/**
 * seed.ts — Demo driver seeding has been DISABLED.
 * Only real registered drivers will appear in search results.
 * To add test data, register a driver through the normal driver registration flow.
 */

console.log("Seed: Demo driver seeding is disabled. Using only real registered drivers.");
